/**
 *
 * @author Zackary Misso
 * 
 */
package tilesAndGraphics;
// THIS CLASS IS DEPRECIATED :: ZACK
public class ManualLevel {
    /*public static TileMap createMap(){
        Tile[][] map=new Tile[15][20];
        for(int i=0;i<20;i++){
            map[0][i]=new Tile(ImageCache.block,16,Tile.BLOCKED,i,0);
            map[14][i]=new Tile(ImageCache.block,16,Tile.BLOCKED,i,14);
            map[13][i]=new Tile(ImageCache.block,16,Tile.BLOCKED,i,13);
        }
        for(int i=0;i<15;i++){
            map[i][0]=new Tile(ImageCache.block,16,Tile.BLOCKED,0,i);
            map[i][19]=new Tile(ImageCache.block,16,Tile.BLOCKED,19,i);
        }
        for(int i=1;i<13;i++)
            for(int f=1;f<19;f++)
                map[i][f]=new Tile(ImageCache.sky,16,Tile.NORMAL,f,i);
        TileMap tileMap=new TileMap(16,null);
        tileMap.setTiles(map);
        tileMap.setNumRows(15);
        tileMap.setNumCols(20);
        //tileMap.setNumRowsToDraw(15);
        //tileMap.setNumColsToDraw(20);
        return tileMap;
    }*/
}